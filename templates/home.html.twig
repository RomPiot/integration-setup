{% extends 'layouts/base.html.twig' %}

{% block body_id %}page-home{% endblock %}

{% block content %}

    <h1>{{ title }}</h1>
    <p>{{ description }}</p>

    <div class="container">
        <div class="row">
            {% for card in cards %}
                {% include 'partials/card.html.twig' %}
            {% endfor %}
        </div>
    </div>
{% endblock %}
